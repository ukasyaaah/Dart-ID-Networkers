// ignore_for_file: dead_code

void main() {
  bool isMarried = false;

  // ignore: dead_code
  var lambe = isMarried ? 'Nais' : 'Yah Jomblo';
  print(lambe);

  if (isMarried) {
    print('Selamat');
  } else {
    print('Cari!');
  }
}
